{
  "version": 2,
  "framework": null,
  "buildCommand": "prisma generate && nest build && mkdir -p public && echo '{}' > public/index.json",
  "outputDirectory": "public",
  "rewrites": [
    { "source": "/(.*)", "destination": "/api" }
  ]
}
