{
  "version": 2,
  "framework": null,
  "installCommand": "pnpm install",
  "buildCommand": "cd apps/be && npx prisma generate && npx nest build",
  "outputDirectory": "public",
  "rewrites": [
    { "source": "/(.*)", "destination": "/api" }
  ]
}
