{
  "version": 2,
  "framework": null,
  "installCommand": "pnpm install",
  "buildCommand": "mkdir -p public && pnpm --filter=crush-tip-be run build",
  "outputDirectory": "public",
  "rewrites": [
    { "source": "/(.*)", "destination": "/api" }
  ]
}
